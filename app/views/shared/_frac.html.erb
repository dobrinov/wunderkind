<%# One stacked fraction. Shared so a widget answer and a question body never
    show the same value in two different notations. %>
<span class="frac"><span><%= numerator %></span><span><%= denominator %></span></span>
