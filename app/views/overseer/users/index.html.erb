<div class="page">
  <header>
    <h1><%= t("overseer.users.title") %></h1>
  </header>

  <main>
    <table>
      <thead>
        <tr>
          <th><%= t("overseer.users.name") %></th>
          <th><%= t("overseer.users.email") %></th>
          <th><%= t("overseer.users.role") %></th>
          <th><%= t("overseer.users.rating") %></th>
          <th><%= t("overseer.users.level") %></th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= t("overseer.users.roles.#{user.role}") %></td>
            <td><%= user.elo %></td>
            <td><%= user.level %> (⭐<%= user.total_xp %>)</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </main>
  <footer>
    <%= paginate @users if @users.respond_to?(:total_pages) %>
  </footer>
</div>
