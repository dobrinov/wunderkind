<!DOCTYPE html>
<html class="h-full bg-gray-100" lang="<%= I18n.locale %>">
  <head>
    <%= render "shared/head" %>
  </head>

  <body class="h-full">
    <%= yield %>
    <%= render "shared/flash" %>
  </body>
</html>
