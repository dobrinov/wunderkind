# Problem types in the bank, and how to make more of each

This is the working reference for the shipped question corpus: what kinds of
problem exist, what each kind is *for*, and the recipe for adding more of it.

The corpus lives in `db/seeds/ladders/*.yml` and is generated by
`tools/authoring` (see `README.md` for the build commands). Today it holds
**22,705 problems in 526 families**, Elo 600–2800:

| answer format | problems | what it is good for |
|---|---|---|
| interactive widget | 12,660 | anything the student can *do*: place, shade, sort, fill, match |
| typed exact value | 9,255 | one number, checked with `ExactValue` (fractions ≡ decimals ≡ percents) |
| multiple choice | 790 | symbolic or verbal answers a typed field cannot compare |
| with a figure | 1,011 | geometry, charts, number lines, solids — drawn to scale from the numbers |

§3–§5 catalogue the problem *types* and §10 lists every family there is, area by
area — that one is the index to read before writing a new family.

## 1. The unit of authoring is a *family*, not a problem

A family is one **type** of problem, written once, at six or seven rising Elo
ratings, with several variants per rung:

```ruby
Authoring.family "pct.of_amount", topic: "Проценти", area: "fractions",
                 rungs: [ 970, 1060, 1150, 1240, 1330, 1430 ], variants: 6 do |c|
  pct  = c.pick(c.by_level([ [ 10, 50 ], [ 10, 20, 25, 50 ], ... ]))   # difficulty
  base = c.int(c.by_level([ 2..12, 2..20, 4..40, ... ]))               # rises per rung
  c.q(text: "...", answer: Num.ans(base * pct / 100), explanation: Explain.build(...))
end
```

Everything else in this document is a catalogue of what can go in the block.

**Rung rules** (enforced by `spec/services/ladder_corpus_spec.rb`):
at least 4 rungs, at least 300 points from bottom to top, never more than 150
points between neighbours, and no empty rung. Close rungs are the point: a
student who fails a type drifts *down that type's ladder* rather than out of it.

## 2. Answer formats and when to use which

| format | use it when | how to write it |
|---|---|---|
| `answer:` | the answer is a single number | `answer: Num.ans(value)` — ASCII minus, comma decimals, fractions as `3/4` |
| `answer:` + `tolerance:` | the answer is rounded (anything with π) | `tolerance: "0.05"` |
| `options:` | the answer is symbolic (`(x+3)(x−2)`), verbal (`да`/`не`), or a formula | 3–4 options, distractors = real mistakes |
| `widget:` | the student should *act*, not type | see the catalogue below |
| `figure:` | a diagram carries part of the information | `Figures.*`, and repeat every number in the text |

Rule of thumb: **numeric → typed, symbolic → options, procedural → widget.**
A typed field compares strings when it cannot parse a number, so `x = 3` or
`2√3` typed by hand will be marked wrong. Never ask for those.

## 3. The interactive catalogue (12 widgets)

Each widget is `WidgetKit.<builder>` in the authoring tool, a checker in
`app/services/widgets.rb`, and a Stimulus controller under
`app/javascript/controllers/widgets/`. Counts are problems currently shipping.

### 3.1 `blanks` — one or more numbered boxes (3,817)
For answers that have **two halves**: both roots, x and y, quotient and
remainder, perimeter and area, НОД and НОК, period and last digit.

```ruby
widget: WidgetKit.blanks([ [ "q", "частно", quotient ], [ "r", "остатък", rest ] ],
                         prompt: "#{dividend} : #{divisor}")
```
*More of it*: any procedure whose method has two outputs. Each field is graded
with `ExactValue`, so `1/2` and `0,5` both pass; pass `tolerance:` for rounded
values. Do not use it for one number — that is a typed answer.

### 3.2 `multi_select` — choose every option that fits (2,011)
For properties that must be judged option by option: divisibility, primality,
equivalent fractions, valid triples, true statements, losing positions.

```ruby
widget: WidgetKit.multi_select([ [ "34", false ], [ "45", true ], [ "51", true ], [ "62", false ] ])
```
*More of it*: pick a property, generate 2–3 numbers that have it and 3 near
misses. **The stem must list the options** (`"Кои от числата 34, 45, 51, 62 ..."`)
— the importer keys questions by their text, so a fixed stem collapses the whole
family into one problem.

### 3.3 `grid_fill` — a table with holes (1,076)
Magic squares, Latin squares, times tables, function machines, place-value
tables, two-way frequency tables, column addition, cross-number puzzles.

```ruby
widget: WidgetKit.grid_fill(rows: [ [ 8, nil, 6 ], [ nil, 5, nil ] ], answers: [ [ 8, 1, 6 ], [ 3, 5, 7 ] ],
                            column_headers: %w[…], row_headers: %w[…])
```
*More of it*: any structure with a **checkable constraint** — rows summing to a
constant, each symbol once per row, a rule linking input to output. Keep grids
at most 5×5 and blanks at most ~8, or the widget stops being a maths exercise.

### 3.4 `categorize` — items into named groups (1,124)
Prime/composite, even/odd, terminating/repeating decimals, acute/obtuse,
possible/impossible, growing/shrinking, winning/losing.

```ruby
widget: WidgetKit.categorize(bins: [ [ "p", "просто" ], [ "c", "съставно" ] ],
                             items: [ [ "i1", "17", "p" ], [ "i2", "21", "c" ], … ])
```
*More of it*: any binary or ternary classification whose rule is worth stating.
Every bin must get at least one item, and the stem must name the items.

### 3.5 `matcher` — pair the two columns (1,021)
Fraction ↔ percent, expression ↔ value, shape ↔ formula, equation ↔ root,
number ↔ binary, event ↔ probability, action ↔ inverse action.

```ruby
widget: WidgetKit.matcher([ [ "1/4", "25%" ], [ "1/2", "50%" ], [ "3/4", "75%" ] ])
```
*More of it*: any one-to-one correspondence with 3 pairs. Labels must be
distinct on both sides, or two answers become right at once.

### 3.6 `coordinate_plot` — click lattice points (835)
Plot a point, complete a rectangle, reflect/translate, mark the midpoint, plot
the intercepts, plot the solution of a system, plot a data point.

```ruby
widget: WidgetKit.plot(points: [ [ 2, 1 ] ], fixed: [ [ -3, 2, "A" ] ], x_range: (-5..5), y_range: (-5..5))
```
*More of it*: anything whose answer is a **position**. The order of placement
never matters; keep every solution point inside the range and off the given
points.

### 3.7 `grid_shade` — colour cells on squared paper (849)
Two modes. *Exact set*: complete a symmetry, mark a knight's moves, shade one
colour class of a chessboard. *Count only*: shade 3/8 of the grid, shade a
figure with area 12, shade 40%.

```ruby
widget: WidgetKit.grid_shade(rows: 4, cols: 8, cells: [ "0,1", "1,2" ])   # exact
widget: WidgetKit.grid_shade(rows: 4, cols: 10, count: 12, given: [ "0,0" ]) # any twelve
```
*More of it*: area and fraction models at the bottom of the ladder; tilings,
parity and colouring arguments at the top.

### 3.8 `ordering` — arrange in sequence (588)
Numbers, negatives, decimals, fractions, mixed notations, powers, measures,
probabilities, steps of a solution, figures by area.

*More of it*: any set with a total order; 3–5 items. Include the items in the
stem for uniqueness.

### 3.9 `number_line` — place a point (459)
Integers, negatives, decimals, fractions, the result of an operation, a rounded
value, the midpoint, a percentage of 100.

*Constraint*: `(max − min) / step ≤ 40` and the answer must sit **on** the step
grid, or the student cannot place it. `check.rb` enforces both.

### 3.10 `clock_hands` — set the time (387)
Read/set a time, elapsed time forwards and backwards, half hours, arithmetic
modulo 12.

### 3.11 `angle_dial` — turn a ray (299)
Set an angle, its complement or supplement, the third angle of a triangle, an
exterior angle, a polygon's interior angle, the angle between clock hands.

*Constraint*: the answer must be a multiple of the dial's step (5° by default;
use `step: 1, tolerance: 2` for angles like 108°).

### 3.12 `fraction_bars` — shade parts of a strip (194)
The first fraction model: shade 3/4, an equivalent fraction, a percentage, a
decimal, the remainder, a sum of like fractions.

## 4. The written-answer catalogue

The typed families (`arithmetic`, `numbers`, `fractions`, `geometry`, `logic`,
`word_problems`, `algebra`, `functions`, `advanced`): one number as the answer,
graded by `ExactValue`. Nothing to click, which is the point — a student who can
only answer when a widget offers the moves has not learnt the arithmetic.

### 4.1 Bare computation — „Пресметни: 16 + 21.“

An expression and nothing else, the plainest problem in the bank. It earns its
place twice over: a student who cannot add two two-digit numbers cannot do
anything above it, and this is the one type whose difficulty is honestly just
the size of the numbers and whether the digits cross a ten.

| type | family | rungs | what rises |
|---|---|---|---|
| sum | `add.column_carry` | 690–1070 | 11–44 with no carry → 15–79 carrying → three digits → four |
| difference | `sub.column_borrow` | 700–1090 | 25–69 no borrow → 22–95 borrowing → three digits → four |
| product, one-digit multiplier | `mul.long_single` | 780–1220 | 12–40 · 2–4 → 23–99 · 3–6 → 2500–9800 · 6–9 |
| product, two-digit multiplier | `mul.long_double` | 900–1340 | 11–25 · 11–19 → 210–980 · 21–75 |
| quotient | `div.long` | 820–1270 | dividends to ~100, divisor 2–9 → dividends to ~29 000, divisor 12–32 |
| missing addend / factor | `add.missing_number`, `div.missing_factor` | 700–1220 | `1 + ☐ = 6` → the same with three- and four-digit numbers |
| several operators | `order.two_ops`, `order.brackets`, `order.four_ops`, `order.powers`, `order.with_negatives` | 850–1560 | one operator → brackets → four operators → powers → negatives |

36 problems each (6 rungs × 6 variants), all in `families/arithmetic.rb`.
**Up to 100** — the band the youngest
students live in — is the bottom rung of each of those (`11–44 + 11–44`,
`25–69 − 25–69`, `12–40 · 2–4`, `~100 : 2–9`) plus, in bulk, the fact tables in
§4.2. Above it the same families keep going: the type does not change when the
numbers get longer, which is exactly what makes it a ladder.

Three rules that belong to this type in particular:

* **`check.rb` computes the stem.** A question of the form `Пресметни: <израз>.`
  is parsed, evaluated and compared with the stated answer — the one place where
  the tool checks the *mathematics* and not just the shape. It works while the
  expression is digits and `+ − · : ² ³ ( )`, so keeping the typography (`·` for
  multiplication, `:` for division, U+2212 for minus) keeps the check.
* **Force the draw to be the type.** `add.column_carry` rejects a pair that does
  not carry on a carrying rung *and* a pair that does on the rung below it;
  `sub.column_borrow` does the same for borrowing. Without that the rung is a
  lottery and half of it teaches the wrong lesson.
* **The explanation is the written algorithm**, digit by digit, with the carry
  named ("пишем 5, наум 1") and a `check:` line that inverts the operation
  (`сборът − второто събираемо`, `частното · делителя`). That is what the
  student who got it wrong needs, and it is why these families sit *next to* the
  fact tables rather than being replaced by them.

### 4.2 The fact tables — drill, generated from a rule

`db/seeds/arithmetic_facts.yml`, outside the ladder corpus and not counted in
the totals at the top of this file: 7,646 statements of the form „Колко е 7 + 8?“,
Elo 663–920, generated from the rule in the file's own header rather than
authored, and carrying **no explanation** — a wrong answer to 7 + 8 needs
practice, not prose.

| table | rows | the rule |
|---|---|---|
| sums to 100 | 2,500 | `a + b ≤ 100`, `1 ≤ a ≤ b` — each commutative pair once |
| differences within 100 | 4,948 | `a − b`, `1 ≤ b < a ≤ 100` |
| the 10×10 times table | 99 | both orders |
| its division facts | 99 | `(b·q) : b`, `b, q ≤ 10` |

They are not families because they are not ladders: a family is one type written
at rising ratings, a fact table is a whole finite set inside one band. Being one
template each, they are also what `SessionComposer::MAX_PER_SHAPE` (= 2) exists
for — without that cap thousands of `Колко е # + #?` rows in a single topic fill
every young session — and what makes `problems:export` a lossy copy of them
(`ProblemSeeds::MAX_PER_SHAPE` = 5 per template, so re-import the file instead).

*More of it*: extend the rule in the header and regenerate; never hand-add rows.
The gaps, in the order they matter:

1. **Multiplication and division within 100 are thin** — 198 statements against
   7,448 for addition and subtraction. The table stops at 10×10 and the division
   facts are its exact inverse, so a child drilling multiplication meets a
   twentieth of the material a child drilling addition does.
2. **Division with a remainder** as drill (`53 : 6`): every division fact in the
   file divides exactly. The *type* is covered in the ladder — `rem.basic` (36),
   `blank.division_remainder` (66, quotient and remainder in two boxes, §3.1),
   `div.share_remainder` (36, as a story) — so what is missing is only the finite
   table of `a : b` with a remainder within 100, if drill is what is wanted.
3. **The mental-arithmetic moves**: complements (`☐ + 37 = 100`), doubles and
   halves, ±10 and ±100 — each a finite set with an obvious rule. (×10 and ×100
   exist as a ladder family, `mul.powers_of_ten`, but not as a table.)
4. The times table to 12×12.

Two constraints on all of it. The stem must carry its numbers, because the
importer keys questions by their text. And the existing multiplication rows say
`×` where the rest of the corpus says `·`: regenerating with the right sign
writes *new* questions and orphans the old ones, so that is a migration to
decide on, not a rebuild to run.

### 4.3 Type groups worth knowing

* **Written algorithms** — column addition/subtraction/multiplication/division,
  with the carry or borrow spelled out in the explanation.
* **Context problems** — shopping, sharing, saving, tickets, recipes, reading.
  Names, goods and containers come from `Props`; keep verbs gender-neutral
  (aorist, no participles), and avoid possessive pronouns.
* **Reverse problems** — from part to whole, from area to side, from sum to
  addend. These are the rungs that catch students who only know the forward move.
* **Two-step problems** — "of the remainder", successive discounts, buy-then-share.
* **Formula work** — perimeter/area/volume, Pythagoras, similarity, circle,
  progressions, derivatives.
* **Classification and comparison** — which is bigger, which fits the rule.
* **Estimation** — round, then compute; the explanation must say how far off it is.

## 5. The competition catalogue (`interactive_puzzles`, Elo 1550–2800)

The types competition papers actually use, each mapped onto a widget:

| type | widget | family |
|---|---|---|
| magic square | grid_fill | `puzzle.magic_square` |
| Latin square / mini-Sudoku | grid_fill | `puzzle.latin_square` (numerals, 1550+); pictures at 850+ in `puzzle.ladybug_square` |
| cage sums (KenKen-like) | grid_fill | `puzzle.cage_sum` |
| addition pyramid | grid_fill | `puzzle.addition_pyramid` |
| cryptarithm (AB + BA) | blanks | `puzzle.cryptarithm_reverse` |
| digit reversal difference | blanks | `puzzle.digit_difference` |
| digits from sum and product | blanks | `puzzle.digits_sum_product` |
| linear Diophantine equation | blanks | `puzzle.diophantine_pair` |
| sum of consecutive integers | blanks | `puzzle.consecutive_sum` |
| last digit / cycle length | blanks | `puzzle.last_digit_cycle` |
| remainders, weekday shifts | blanks | `puzzle.remainder_pair`, `puzzle.weekday_shift` |
| divisor counting | blanks | `puzzle.divisor_count` |
| Nim (winning move, losing set) | blanks, multi_select | `puzzle.nim_move`, `puzzle.nim_positions` |
| Tower of Hanoi | blanks | `puzzle.hanoi_moves` |
| pigeonhole | blanks | `puzzle.pigeonhole_pair` |
| weighings (ternary search) | blanks | `puzzle.weighings` |
| knight moves | grid_shade | `puzzle.knight_moves` |
| domino tiling parity | multi_select | `puzzle.domino_parity` |
| chessboard colouring | grid_shade | `puzzle.chessboard_colour` |
| lattice geometry, Pick's theorem | plot, blanks | `puzzle.lattice_rectangle`, `puzzle.pick_theorem` |
| Gauss pairing, odd/even sums | blanks | `puzzle.gauss_sum`, `puzzle.square_sum_pattern` |
| telescoping sums | blanks | `puzzle.telescoping` |
| Fibonacci-style recursion | blanks | `puzzle.fibonacci_step` |
| parity invariants | categorize | `puzzle.sort_possible` |
| binary representation | matcher | `puzzle.match_base_two` |
| perfect squares | multi_select | `puzzle.pick_perfect_squares` |

**Untapped types worth adding next**: water-pouring states, river crossings,
handshake/graph degree arguments, colouring proofs on triangular grids,
invariants with tokens, monovariants, extremal arguments ("what is the largest
n such that…"), 3-4-5 style constructions, clock-hand coincidences, digit sums
modulo 9 as a checksum, base-n arithmetic, Catalan-flavoured path counting
(the monotone staircase case is built — §5c), and
inclusion–exclusion with three sets.

## 5b. Types taken from competition papers

These came from problems pasted in one at a time and generalised into ladders.
Two of them need a **solver**, not just a formula: the builder enumerates every
candidate and keeps the problem only if the answer is forced. That is the rule
for any deduction type — a puzzle with two answers is not a puzzle.

| type | widget | family | how the builder guarantees it |
|---|---|---|---|
| who-is-who from a fixed tally ("three solved 3, two solved 2") | multi_select, categorize, blanks | `deduce.minority_group`, `deduce.assign_all`, `deduce.two_people` | enumerates all assignments of the tally, keeps the ones where exactly one satisfies the clues; the explanation is produced by *deducing*, and a puzzle whose derivation the narrator cannot finish is dropped |
| circular seating, forced neighbours | multi_select | `logic.circle_neighbours` | enumerates all seatings (rotations collapsed, reflections kept — the clues say clockwise); asks only about a person whose neighbours are the same in every surviving seating, and only about someone the adjacency clues do not name |
| two kinds of creature, two attribute totals | blanks | `logic.two_creatures` | searches for other non-negative integer solutions and rejects the problem if one exists |
| extremal share ("each ate at least 2 — how many could one eat?") | blanks | `logic.extremal_share` | asks for the others' minimum *and* the maximum, so the method is visible |
| digit product and the zero trap | blanks, multi_select | `digits.sum_and_product`, `digits.zero_product_pick` | zeros get commoner up the ladder; the `watch` line changes depending on whether this variant contains one |
| counting under digit constraints | blanks | `count.digit_choices` | asks for both factors and the product — the multiplication principle made explicit |
| Latin square drawn with ladybugs, one line of it asked | figure + blanks | `puzzle.ladybug_square` | enumerates every Latin square consistent with the clues and keeps the problem only if the asked cells hold the same ladybug in all of them — the grid itself may still have several completions, because it is the *question* that has to have one answer; then re-derives the answer by row/column elimination aimed at the asked cells, and drops the problem if that narration cannot finish, so the explanation is the chain the builder actually walked. Difficulty is the length of that chain — how many cells outside the asked line have to be filled first — not the size of the grid, which stops at 4x4 |
| arithmetic on pictures (lines, not parts) | figure + typed | `fig.square_line_equation` | the explanation contrasts counting lines with counting parts, which is where the trap is |
| row with a neighbour condition ("each child stands between a girl and a boy") | blanks, grid_shade, multi_select | `logic.row_between`, `shade.row_between`, `pick.row_possible` | the condition is on a child's *neighbours*, so positions two apart alternate and the row splits into two chains; even lengths give one row, n ≡ 1 (mod 4) forces the count but not the row, n ≡ 3 (mod 4) is impossible — one problem, three rungs of reasoning |
| age with a time shift | blanks | `logic.age_shift`, `logic.age_sum_shift` | going back to the present *before* using the sum; the second family isolates the trap that a sum of k ages grows by k·years |
| most items within a budget | blanks, multi_select | `logic.max_items_budget`, `pick.affordable_sets` | greedy from the cheapest, with the leftover money asked for so the method shows |
| exact payment from given coins | multi_select | `pick.exact_payment` | subset sums of the coin multiset; coin sets are chosen without a 1 so some amounts are genuinely unreachable |

**Clue kinds available to the deduction builder**: direct ("X solved 3"),
negative ("X did not solve 3"), comparison ("X solved more than Y"), equality
("X and Y solved the same"), difference ("X and Y solved different amounts"),
and for circles "X stands immediately after Y" and "X and Y are not neighbours".
Clues may share people — forbidding that makes whole configurations impossible.

## 5c. Path counting on a staircase (Кенгуру №26)

The type: a figure made of unit squares, left aligned, never narrowing
downwards. The kangaroo sits in the top-left square **А**, its mother in the
bottom-right square **Б**, and it moves from square to square *only right and
down*. How many different paths are there? (The plain rectangle, typed and
without a figure, was already in the bank as `count.grid_paths` in `logic`,
§10.19 — a new path-counting family belongs beside one of the two, not next to
both.) It is the cheapest problem in the
corpus that needs a table rather than a formula: the number of paths into a
square is the number into the square above plus the number into the square to
its left, because those are the only two ways in.

```ruby
# Фигурата е съставена от квадратчета, подравнени отляво: в първия — 2, във
# втория — 3, в третия — 3. Кенгурчето е в квадратчето А (горе вляво), а майка
# му — в квадратчето Б (долу вдясно). Кенгурчето се движи от квадратче в
# квадратче само надясно и надолу. По колко различни пътя може да стигне до
# майка си?                                                      → 5
```

**Three answer formats, one type** — 106 problems, all with a figure. The typed
count is the primary shape; the other two exist because the *method* is worth
making the student perform:

| family | format | rungs | what it asks |
|---|---|---|---|
| `paths.staircase` | typed + `figure:` | 1150–1930 (7) | the number of paths from А to Б |
| `paths.staircase_table` | `grid_fill` + `figure:` | 1050–1570 (5) | the count table itself — 1s pre-filled down the first row and column, squares outside the figure (and the crossed-out one) pre-filled `0`, the rest blank |
| `paths.staircase_moves` | `blanks` + `figure:` | 1200–1850 (6) | the count *and* how many moves a path takes — `(rows − 1) + (last width − 1)`, the same for every path, which is exactly what the student who answers "6 пътя, защото 6 хода" has confused |

Topic `Броене и комбинаторика`, area `interactive_kangaroo`, all three in
`families/interactive_kangaroo.rb` behind one `Staircase` module. The `grid_fill`
variant stops at 1570 and at a 4×4 grid: at most 5×5 and 8 blanks, or the widget
stops being a maths exercise (§3.3).

**The ladder** — the count is not the difficulty. The number of rows is, and so
is a crossed-out square; a count above ~30 matters only because it is what stops
a student listing the paths one by one instead of filling the table. What each
rung of `paths.staircase` draws:

| Elo | rows | widths up to | crossed out | count in |
|---|---|---|---|---|
| 1150 | 2 | 6 | — | 3–6 (one path drawn in, as the sheet does) |
| 1280 | 3 | 4 | — | 4–15 |
| 1410 | 3 | 5 | — | 6–30 |
| 1540 | 4 | 5 | — | 8–45 |
| 1670 | 4 | 6 | — | 12–80 |
| 1800 | 4 | 6 | one square | 8–60 |
| 1930 | 5 | 6 | one square | 15–130 |

Each rung also carries a `min_last` — the last row has to be at least so wide.
That is not difficulty, it is what keeps a rung's shapes out of the space of the
rung below: the importer keys questions by their text, so two rungs drawing from
the same pool of figures leave the upper one empty rather than merely repetitive.

**What the builder has to guarantee.** Like the deduction families in §5b, this
one needs a solver rather than a formula — a staircase with a hole has no
binomial — and the solver is checked against brute force:

1. Widths **non-decreasing** downwards, first ≥ 2, last ≤ 6, 2–5 rows. That is
   what makes Б exist and makes it reachable; a figure that narrows has no
   bottom-right square to walk to.
2. The table's answer is compared with a **full enumeration** of the paths
   (≤ 30 squares, so walking every path costs nothing) and the variant is
   dropped on a mismatch. Two counts of one thing, one of them dumb.
3. Count between 3 and ~120: two paths is not a question, and nobody verifies
   a hundred and fifty.
4. Above the bottom rung, at least one strict step in the widths — otherwise it
   is a rectangle and the answer is a binomial coefficient the top of the ladder
   can look up.
5. A crossed-out square sits strictly inside: never А or Б, never in the first
   or last row (there it would only shorten the figure), and never in the left
   column, which is where the method's column of 1s comes from — the explanation
   says "по лявата колона се стига само по един път" and that has to be true.
6. The example path, drawn only on the bottom rungs, is taken *from* the
   enumeration, so it cannot be an illegal path.
7. The stem spells out the row widths and the blocked square in words. The views
   render question images with an empty `alt`, and the importer keys questions by
   their text — one sentence does both jobs.

**Explanation and hints.** The explanation fills the table row by row (a crossed
square prints as `×`) and ends on the number in Б; `watch:` names the two
mistakes the type is built to catch — multiplying instead of adding, and
answering with the number of *moves* (constant) instead of the number of paths.
`check:` re-adds the two squares feeding Б, and has to say when there is no
square above it at all: in a staircase the row above Б is usually narrower, and
"the number above it (0)" would send the student looking for a square that is
not there.
The hint ladder is the method without a number in it: *into a square you come
only from above or from the left, so add those two*; then *write 1 in А and fill
the whole first row and first column*; then *fill row by row — a square outside
the figure, or a crossed-out one, contributes 0*. None of them states the count.

**The figure** — `Figures.staircase_grid(widths:, blocked: [], path: nil)`,
drawn like the printed sheet: thin interior lines, a **bold outline** on every
edge whose neighbour square is missing, А shaded in `FILL` with an indigo
letter, Б in `ACCENT_FILL` with a red one, a blocked square grey with a cross,
and the optional example path in `ACCENT`, trimmed by about a third of a square
at both ends so it neither covers the letters nor lands on the mother — the
arrow stops at the edge of her square, as the original does. A crossed-out
square keeps a white ground: `rasterize.sh` quantizes to 32 colours and returns
a light grey as dither noise, so the cross carries the meaning on its own.

## 6. The figure catalogue

`Figures.*` (in `lib/figures.rb`) draws: number lines, fraction strips, grids,
dot arrays, tape diagrams, bar charts, pie charts, spinners, tables, clocks,
triangles (from sides *or* from angles), quadrilaterals with heights,
L-shapes, circles with sectors, angle pairs (vertical, supplementary, around a
point, parallel + transversal), single angles, regular polygons, similar
triangles, segment chains, coordinate planes with lines and parabolas, cuboids,
cylinders, cones, spheres, prisms, pyramids, nets, Venn diagrams, growing
patterns and ladybug Latin squares (`ladybug_square`, whose `ladybug` helper
draws a bug with 1..5 spots at any scale).

*More of it*: add a builder that returns `Svg.canvas(w, h) { |c| … }`, then use
it with `figure:`. Two rules: **draw to scale from the numbers in the question**
(a 56° angle must look like 56°), and **repeat every number in the text**,
because the views render question images with an empty `alt`.

## 7. Recipe: add a family

1. Pick the file by area (`families/interactive_number.rb`, `families/geometry.rb`, …).
2. Choose a topic from `db/seeds/topics.yml` (the tree is fixed; no new topics).
3. Choose rungs: start where the type first makes sense, step 60–150 points.
4. Draw parameters with `c.by_level([...])` so difficulty rises with the rung.
5. Reject impossible or silly draws with `raise Authoring::Duplicate` (a ladder
   with a 178 m ladder in it is worse than a shorter ladder).
6. Write the explanation with `Explain.build(idea:, steps:, answer:, check:, watch:)`.
   `watch:` names the mistake the problem is designed to catch — the student
   only ever sees the explanation after getting it wrong.
7. Add a hint ladder with `hints: [...]` — 2–3 rungs, cheapest nudge first, and
   **none of them the answer**. A hint is read *instead of giving up*, an
   explanation *after* getting it wrong; they are not the same text at two
   lengths. The shape that works: the rule or method (no numbers), then the
   first concrete move with this variant's numbers, then the last rung that
   leaves exactly one step for the student. `check.rb` rejects a rung that
   contains the typed answer, or that names a blank and its value close
   together. Imported ladders reach students without further review, so a
   leaked answer ships silently — write the rungs as questions where you can
   ("колко остава за тази клетка?") rather than as statements.
8. Rebuild, check, import:

```bash
ruby tools/authoring/build.rb          # regenerate the YAML
tools/authoring/rasterize.sh           # only if figures changed
ruby tools/authoring/check.rb          # validation
bin/rails problems:import FILE=db/seeds/ladders
bundle exec rspec spec/services/ladder_corpus_spec.rb
```

## 8. Recipe: add a widget

1. Checker + display in `app/services/widgets.rb` (the solution must be
   decidable from the submitted state alone).
2. Stimulus controller in `app/javascript/controllers/widgets/`, registered in
   `controllers/index.js`. Leave the hidden `state` field **empty until the
   answer is complete** — that is what keeps the submit button honest.
3. Partial in `app/views/widgets/_<key>.html.erb`; shuffle anything whose order
   could leak the answer.
4. Hints in `config/locales/app.bg.yml` and `app.en.yml`.
5. A demo in `app/views/design_system/show.html.erb`.
6. Specs in `spec/services/widgets_spec.rb`; validation in `tools/authoring/check.rb`.
7. A builder in `tools/authoring/lib/widget_kit.rb`.

The admin form can only build `QuestionFormParams::EDITABLE_WIDGETS`
(number line, ordering, fraction bars). Questions using the other widgets keep
their grading when edited in the form, so nothing is lost — but the parameters
are meant to be generated, not typed.

## 9. Quality rules (what `check.rb` and the specs enforce)

* **Unique text.** The importer keys questions by their text; two problems with
  the same stem are one problem. Put the numbers in the stem, always.
* **Parseable answers.** Typed answers must match `ExactValue`'s grammar.
* **Distractors are mistakes**, not noise: the off-by-one, the forgotten carry,
  the sum instead of the product.
* **Explanations**: at least 120 characters, numbered steps, a check line, and
  where possible a `watch` line.
* **Typography**: `−` (U+2212) in text, ASCII `-` in answers, decimal comma,
  `·` for multiplication, `:` for division.
* **Realism**: no 178 m ladders, no cyclists at 140 km/h, no ten-hour films.
  Cap the ranges instead of letting the top rung run away.
* **Reachability**: number-line answers on the step grid, dial angles on the
  dial's step, plot points inside the plane, grid counts inside the grid.

## 10. The areas, one by one

An *area* is a file in `families/`, not a topic: it records which batch a family
was written in, while topics cut across every file — the same fraction is typed
in `fractions`, drawn in `figures` and shaded in `interactive_number`, once per
format. §3–§5 say what each format is *for*; this section says what is already
covered, so that a new family adds a type instead of a synonym for one.

Read it before writing a family, and grep it for the noun you are about to use.
The lists are exact as of the last rebuild (526 families) and go stale the
moment one is added, so `grep -h "Authoring.family" tools/authoring/families/*.rb` is
the authority whenever a name here is missing.

### 10.1 `advanced` — 1,228 problems, 35 families, Elo 1060–2250

The top of the corpus: progressions, probability, statistics, trigonometry,
solving triangles, solids of revolution and a first look at derivatives. All
typed.

* **Аритметична прогресия** (1330–1910) — `ap.find_difference`, `ap.nth_term`,
  `ap.sum`, `ap.theatre_word`
* **Геометрична прогресия** (1400–1980) — `gp.doubling_word`, `gp.nth_term`,
  `gp.ratio`, `gp.sum`
* **Производна** (1700–2250) — `deriv.extremum`, `deriv.polynomial`,
  `deriv.tangent_slope`, `deriv.velocity`
* **Статистика** (1060–1760) — `stat.mean`, `stat.median`, `stat.range_mode`,
  `stat.weighted_mean`
* **Решаване на триъгълник** (1580–2180) — `tri.area_with_sine`,
  `tri.cosine_rule`, `tri.height_from_area`, `tri.sine_rule`
* **Ротационни тела** (1500–2080) — `cone.volume`, `cyl.surface`, `cyl.volume`,
  `sphere.volume`
* **Вероятност** (1140–1860) — `prob.balls`, `prob.complement`,
  `prob.dice_condition`, `prob.two_events`
* **Тригонометрични функции** (1470–2060) — `trig.find_side`, `trig.identity`,
  `trig.ratio_in_triangle`, `trig.special_value`
* **Призма и пирамида** (1380–1940) — `prism.surface_cube_scaled`,
  `prism.volume_rect_base`, `pyramid.volume`

Statistics and probability are the parts of this file a middle-school student
reaches — `stat.mean` from 1060, `prob.balls` from 1140 — while the progressions
start at 1330, the solids of revolution at 1500 and the derivatives at 1700.
Every family whose answer is rounded carries `tolerance:`: all of `cone.*`,
`cyl.*` and `sphere.*`, and the parts of `tri.*` and `stat.*` that divide.

### 10.2 `algebra` — 1,029 problems, 29 families, Elo 1080–2010

Quadratic equations, inequalities, systems, powers and roots, rational
expressions.

* **Степени и корени** (1080–1910) — `pow.evaluate`, `pow.negative_exponent`,
  `pow.power_of_power`, `pow.same_base`, `pow.scientific`, `sqrt.equation`,
  `sqrt.perfect`, `sqrt.simplify`
* **Квадратни уравнения** (1380–2010) — `quad.area_word`, `quad.count_roots`,
  `quad.discriminant`, `quad.factored_roots`, `quad.solve_integer_roots`,
  `quad.vieta`
* **Рационални изрази** (1280–1960) — `rat.difference_of_squares`,
  `rat.evaluate`, `rat.expand_square`, `rat.factor_common`,
  `rat.factor_trinomial`, `rat.undefined_value`
* **Неравенства** (1250–1910) — `ineq.brackets`, `ineq.budget_word`,
  `ineq.count_integers`, `ineq.linear_smallest`, `ineq.negative_coefficient`
* **Системи уравнения** (1330–1960) — `sys.ages_word`, `sys.elimination`,
  `sys.sum_difference`, `sys.tickets_word`

Six families are multiple choice, and for one reason: a factorisation, an
expansion, `√12` or standard form is a *symbolic* answer, and a typed field
would compare it as a string (§2).

### 10.3 `arithmetic` — 1,080 problems, 30 families, Elo 660–1560

The four operations and the order of operations. The bare-computation families
are documented in §4.1 and the drill tables beside them in §4.2; what is left
here is the same arithmetic wrapped in a situation — change from a purchase,
comparing two amounts, equal groups, unit price, durations, lengths,
temperature.

* **Събиране и изваждане** (660–1320) — `add.change`, `add.column_carry`,
  `add.compare_difference`, `add.duration`, `add.estimate`,
  `add.missing_number`, `add.pyramid`, `add.three_numbers`,
  `add.two_step_story`, `add.units_length`, `sub.column_borrow`,
  `sub.temperature`
* **Умножение и деление** (700–1360) — `div.how_many_boxes`, `div.long`,
  `div.missing_factor`, `div.share_remainder`, `div.unit_price`,
  `mul.equal_groups`, `mul.long_double`, `mul.long_single`,
  `mul.powers_of_ten`, `mul.price_total`, `mul.rows_columns`,
  `mul.two_step_story`
* **Ред на действията** (850–1560) — `order.brackets`, `order.four_ops`,
  `order.powers`, `order.two_ops`, `order.which_expression`,
  `order.with_negatives`

The story families are where `Props` earns its place: names, goods and
containers come from there, so 36 variants of one type do not all buy pencils.

### 10.4 `figures` — 801 problems, 41 families, Elo 700–1900

Everything that ships with a drawing: one family per kind of figure, five rungs
of twenty problems each. The figure builders themselves are catalogued in §6.

* **Площ** (780–1560) — `fig.grid_area`, `fig.lshape_area`,
  `fig.parallelogram_area`, `fig.rectangle_area`, `fig.trapezoid_area`,
  `fig.triangle_area`
* **Ъгли** (860–1540) — `fig.angle_classify`, `fig.angles_around_point`,
  `fig.isosceles_base_angles`, `fig.parallel_transversal`,
  `fig.triangle_angles`, `fig.vertical_angles`
* **Призма и пирамида** (1330–1810) — `fig.net_surface`, `fig.prism_volume`,
  `fig.pyramid_volume`
* **Статистика** (900–1510) — `fig.bar_mean`, `fig.bar_read`, `fig.table_read`
* **Дроби** (800–1310) — `fig.fraction_strip_read`, `fig.grid_fraction`
* **Линейна функция** (1150–1740) — `fig.plane_point`, `fig.plane_slope`
* **Обем** (1050–1480) — `fig.cube_surface`, `fig.cuboid_volume`
* **Окръжност** (1230–1760) — `fig.circle_circumference`, `fig.circle_sector`
* **Ротационни тела** (1480–1900) — `fig.cone_volume`, `fig.cylinder_volume`
* **Събиране и изваждане** (820–1240) — `fig.clock_duration`, `fig.segments`
* **Числа и редици** (700–1210) — `fig.number_line_read`, `fig.pattern_next`
* **Вероятност** (1120–1480) — `fig.spinner_probability`
* **Квадратна функция** (1520–1880) — `fig.parabola_vertex`
* **Логически задачи** (1230–1590) — `fig.venn_counts`
* **Периметър** (800–1160) — `fig.rectangle_perimeter`
* **Питагорова теорема** (1290–1650) — `fig.pythagoras`
* **Подобни фигури** (1350–1710) — `fig.similar_triangles`
* **Проценти** (1080–1440) — `fig.pie_percent`
* **Текстови задачи** (950–1310) — `fig.tape_word`
* **Умножение и деление** (700–1060) — `fig.dot_array`

Two rules hold across the whole file. Every number in the drawing is repeated
in the text, because the views render question images with an empty `alt`; and
the figure is drawn to scale from those numbers, so a 56° angle looks like 56°.

### 10.5 `fractions` — 1,113 problems, 31 families, Elo 830–1760

The three notations for the same quantity — fractions, decimals, percents — and
the families that convert, compare and compute in each.

* **Десетични числа** (900–1660) — `dec.add_sub`, `dec.average`, `dec.compare`,
  `dec.divide_by_decimal`, `dec.divide_int`, `dec.money_story`,
  `dec.multiply_decimal`, `dec.multiply_int`, `dec.round`, `dec.to_fraction`,
  `dec.units_convert`
* **Дроби** (830–1710) — `frac.add_same_denominator`, `frac.add_unlike`,
  `frac.compare`, `frac.divide`, `frac.mixed_number`, `frac.multiply`,
  `frac.of_amount_story`, `frac.of_fraction`, `frac.remaining_two_steps`,
  `frac.reverse_whole`, `frac.simplify`
* **Проценти** (970–1760) — `pct.discount`, `pct.exam_score`,
  `pct.fraction_percent`, `pct.increase`, `pct.interest`, `pct.of_amount`,
  `pct.reverse_whole`, `pct.two_discounts`, `pct.what_percent`

`ExactValue` treats `1/2`, `0,5` and `50%` as equal, so a typed answer never
turns on which notation the student reaches for. Where the *form* is the point,
the stem has to say so — „запиши съкратена дроб“, „като смесено число“ — and
those families ask for it in exactly those words.

### 10.6 `functions` — 648 problems, 18 families, Elo 1230–2100

Linear, quadratic, exponential and logarithmic functions: evaluate, find the
zero, find the vertex, solve for x, and one word problem per family group.

* **Линейна функция** (1230–1860) — `lin.evaluate`, `lin.find_x`,
  `lin.parallel`, `lin.slope_two_points`, `lin.taxi_word`, `lin.zero`
* **Квадратна функция** (1420–2060) — `quadf.max_area`, `quadf.roots_count`,
  `quadf.value`, `quadf.vertex`
* **Логаритмична функция** (1530–2100) — `log.evaluate`, `log.product_rule`,
  `log.solve_argument`, `log.to_exponential`
* **Показателна функция** (1470–2050) — `exp.compound_interest`,
  `exp.doubling_word`, `exp.evaluate`, `exp.solve`

Three families are multiple choice because their answer is an equation rather
than a number (`lin.parallel`, `log.to_exponential`) or a count of
intersections (`quadf.roots_count`).

### 10.7 `geometry` — 1,411 problems, 40 families, Elo 780–1960

The school geometry sequence, typed, in centimetres: perimeter, area, volume,
circle, Pythagoras, similarity, angles.

* **Площ** (850–1760) — `area.composite`, `area.parallelogram`,
  `area.path_frame`, `area.rectangle`, `area.rectangle_reverse`,
  `area.trapezoid`, `area.triangle`, `area.units`
* **Ъгли** (900–1710) — `angle.clock`, `angle.complement_supplement`,
  `angle.exterior`, `angle.isosceles`, `angle.parallel_lines`,
  `angle.polygon_sum`, `angle.triangle_sum`, `angle.vertical_pairs`
* **Периметър** (780–1360) — `perim.fence_story`, `perim.rectangle`,
  `perim.regular_polygon`, `perim.square_reverse`, `perim.triangle`
* **Питагорова теорема** (1260–1880) — `pyth.hypotenuse`, `pyth.is_right`,
  `pyth.ladder`, `pyth.leg`, `pyth.rectangle_diagonal`
* **Обем** (1030–1760) — `vol.cube`, `vol.cuboid`, `vol.prism`,
  `vol.surface_cuboid`, `vol.units_litres`
* **Окръжност** (1200–1960) — `circle.area`, `circle.circumference`,
  `circle.in_square`, `circle.radius_from_circumference`, `circle.sector`
* **Подобни фигури** (1280–1880) — `similar.area_ratio`, `similar.map_scale`,
  `similar.missing_side`, `similar.shadow`

Geometry appears in three files, split by *format* rather than by content: the
same triangle is typed here, drawn in `figures`, and clicked or dialled in
`interactive_geometry`. The circle families pass `tolerance:` (π ≈ 3,14); the
reverse families (`perim.square_reverse`, `area.rectangle_reverse`) are the
rungs that catch a student who only knows the forward formula.

### 10.8 `interactive` — 845 problems, 26 families, Elo 640–1780

The first interactive file, and deliberately narrow: only the three widgets
whose answer is a *position* or an *order* — number line, ordering, fraction
bars.

* **Дроби** (780–1610) — `bars.equivalent`, `bars.of_amount`, `bars.remaining`,
  `bars.shade_fraction`, `bars.sum_of_fractions`, `line.place_fraction`,
  `sort.fractions`
* **Числа и редици** (640–1500) — `line.place_integer`, `line.place_midpoint`,
  `line.place_negative`, `line.place_rounded`, `sort.integers`,
  `sort.negatives`
* **Десетични числа** (960–1570) — `bars.shade_decimal`, `line.place_decimal`,
  `sort.decimals`, `sort.measures`
* **Проценти** (1030–1700) — `bars.shade_percent`, `line.place_percent`,
  `sort.mixed_forms`
* **Събиране и изваждане** (700–1180) — `line.place_difference`,
  `line.place_sum`
* **Вероятност** (1280–1730) — `sort.probabilities`
* **Логически задачи** (1150–1600) — `sort.solution_steps`
* **Степени и корени** (1330–1780) — `sort.powers`
* **Умножение и деление** (800–1230) — `line.place_product`

It is also where the youngest students meet those three, below
`interactive_early`'s floor: `line.place_integer` starts at 640.

### 10.9 `interactive_algebra` — 1,442 problems, 24 families, Elo 1020–1990

Algebra and functions with a widget: two-part answers in `blanks`, graphs in
`coordinate_plot`, tables of values in `grid_fill`, and „избери всички“ over
solution sets.

* **Линейна функция** (1250–1870) — `blank.line_from_points`,
  `match.function_values`, `plot.intercepts`, `plot.line_points`,
  `sortbins.function_direction`, `table.linear_values`
* **Аритметична прогресия** (1300–1850) — `blank.progression_start`,
  `pick.arithmetic_sequences`, `table.sequence_terms`
* **Системи уравнения** (1380–1950) — `blank.system_xy`, `pick.system_pairs`,
  `plot.system_solution`
* **Уравнения** (1020–1650) — `blank.equation_steps`,
  `match.equation_solution`, `pick.equation_solutions`
* **Неравенства** (1250–1750) — `blank.integer_bounds`,
  `pick.inequality_solutions`
* **Квадратни уравнения** (1450–1970) — `blank.quadratic_roots`,
  `sortbins.roots_count`
* **Степени и корени** (1350–1830) — `blank.power_forms`, `pick.equal_powers`
* **Геометрична прогресия** (1380–1830) — `match.sequence_next`
* **Квадратна функция** (1540–1990) — `blank.vertex_pair`
* **Рационални изрази** (1400–1850) — `blank.expand_square`

The pattern worth copying: an algebra problem usually has two outputs — both
roots, x and y, slope and intercept, the vertex's two coordinates — and
`blanks` asks for both, which is what stops a student answering half the
question and being marked right.

### 10.10 `interactive_data` — 1,039 problems, 17 families, Elo 1050–1900

Probability, counting, statistics and everyday logic with widgets: outcome
grids, two-way tables, frequency tables, a scatter point to place, events to
sort by how likely they are.

* **Вероятност** (1050–1750) — `blank.probability_fraction`,
  `match.event_probability`, `pick.dice_outcomes`, `shade.probability_area`,
  `sortbins.event_chance`
* **Статистика** (1150–1850) — `blank.mean_median`, `pick.mean_sets`,
  `plot.data_point`, `table.frequency`, `table.two_way`
* **Логически задачи** (1150–1900) — `blank.age_puzzle_pair`,
  `match.riddle_answer`, `pick.logic_consequences`, `sortbins.statements_true`
* **Броене и комбинаторика** (1200–1850) — `blank.count_two_ways`,
  `pick.counting_statements`, `table.outcome_grid`

Probabilities are answered as fractions, which `ExactValue` compares by value —
so `2/6` and `1/3` both pass unless the stem asks for the reduced form.

### 10.11 `interactive_early` — 535 problems, 11 families, Elo 600–1210

The floor of the corpus: Elo 600–930, eleven families, nine of the twelve
widgets, so a six-year-old meets almost every mechanic in the app while the
mathematics stays at counting, comparing and halving.

* **Числа и редици** (600–930) — `early.compare_bins`, `early.hundred_chart`,
  `early.line_neighbours`, `early.pick_even`, `early.sort_three`
* **Събиране и изваждане** (600–930) — `early.blank_add_sub`,
  `early.clock_oclock`, `early.count_cells`, `early.match_ten_pairs`
* **Линейна функция** (860–1210) — `early.plot_first_quadrant`
* **Дроби** (600–930) — `early.shade_half`

No dial, no number line and no fraction bars here — those three meet the
youngest students in `interactive` instead, whose bottom rungs reach just as
low.

### 10.12 `interactive_extra` — 1,442 problems, 22 families, Elo 780–1900

The overflow file: where a widget was under-used, a family was written to spend
it. A clock doing modular arithmetic, `grid_shade` drawing a factor rectangle,
`categorize` sorting numbers by what they round to, `grid_fill` doing column
addition digit by digit.

* **Събиране и изваждане** (780–1400) — `clock.half_hours`,
  `table.column_addition`, `table.subtraction_check`
* **Делимост** (1000–1700) — `pick.divisible_by_two_rules`,
  `shade.factor_rectangle`
* **Десетични числа** (1050–1600) — `match.decimal_fraction`,
  `sortbins.decimal_fraction`
* **Дроби** (1000–1650) — `pick.fraction_gt_half`, `table.fraction_operations`
* **Остатъци** (1050–1650) — `clock.modular`, `shade.remainder_model`
* **Площ** (1150–1650) — `plot.polygon_vertex`, `sort.areas`
* **Ред на действията** (1050–1600) — `pick.true_equations`,
  `sort.operation_results`
* **Умножение и деление** (820–1350) — `match.operation_inverse`,
  `shade.multiplication_model`
* **Квадратна функция** (1450–1900) — `plot.function_point`
* **Проценти** (1200–1650) — `sortbins.percent_more_less`
* **Числа и редици** (1000–1450) — `sortbins.rounding_target`
* **Ъгли** (950–1400) — `sort.angles_size`
* **Подобни фигури** (1300–1750) — `plot.symmetric_pair`

It is the answer to a specific failure mode — a widget with three families
behind it is dead code in the answer flow — so it is organised by mechanic
rather than by topic, and that is fine.

### 10.13 `interactive_geometry` — 1,509 problems, 26 families, Elo 820–1850

Geometry with widgets. The angle dial is nearly this file's own — seven
families turn a ray — and the rest is `coordinate_plot` for transformations,
`grid_shade` for area, perimeter and symmetry, and `blanks` for the pairs
(perimeter and area, hypotenuse and leg).

* **Ъгли** (820–1800) — `blank.triangle_angles_three`, `dial.clock_angle`,
  `dial.complement`, `dial.exterior_angle`, `dial.polygon_interior`,
  `dial.set_angle`, `dial.triangle_third`, `dial.turn_direction`,
  `sortbins.angle_kind`
* **Площ** (880–1700) — `blank.rect_perimeter_area`, `match.shape_formula`,
  `pick.area_units`, `plot.rectangle_vertex`, `shade.rectangle_area`
* **Подобни фигури** (1050–1750) — `plot.midpoint`, `plot.reflect_point`,
  `plot.translate_point`, `shade.symmetry`
* **Периметър** (1100–1600) — `shade.perimeter_shape`,
  `sortbins.triangle_by_sides`
* **Питагорова теорема** (1350–1850) — `blank.pythagoras_pair`,
  `pick.right_triangles`
* **Дроби** (1000–1450) — `shade.fraction_of_shape`
* **Линейна функция** (950–1400) — `plot.point_pair`
* **Обем** (1250–1700) — `blank.cuboid_pair`
* **Призма и пирамида** (1200–1650) — `match.solid_faces`

`dial.*` answers must land on the dial's step: 5° by default, or `step: 1,
tolerance: 2` for an angle like 108° (§3.11). `check.rb` refuses the ones that
cannot be reached.

### 10.14 `interactive_kangaroo` — 927 problems, 18 families, Elo 900–2350

Types taken from competition papers one at a time and generalised into ladders.
Documented family by family in §5b, with the path-counting trio in §5c.

* **Логически задачи** (1000–2350) — `fig.square_line_equation`,
  `logic.age_shift`, `logic.age_sum_shift`, `logic.circle_neighbours`,
  `logic.extremal_share`, `logic.row_between`, `logic.two_creatures`,
  `pick.exact_payment`, `pick.row_possible`, `shade.row_between`
* **Броене и комбинаторика** (1050–2000) — `count.digit_choices`,
  `paths.staircase`, `paths.staircase_moves`, `paths.staircase_table`
* **Числа и редици** (900–1780) — `digits.sum_and_product`,
  `digits.zero_product_pick`
* **Текстови задачи** (1150–2050) — `logic.max_items_budget`,
  `pick.affordable_sets`

### 10.15 `interactive_logic` — 245 problems, 4 families, Elo 850–1950

The four deduction families (`deduce.*`): a fixed tally of results, clues, and
an answer that has to be forced. Documented in §5b — the builder enumerates
every assignment and keeps only the puzzles with exactly one solution.

* **Логически задачи** (850–1950) — `deduce.assign_all`,
  `deduce.minority_group`, `deduce.two_people`, `puzzle.ladybug_square`

### 10.16 `interactive_measure` — 1,338 problems, 22 families, Elo 820–1800

Measures and rates: units, money and change, speed and meeting problems, work,
ratio, recipes, calendars, temperature.

* **Десетични числа** (820–1550) — `blank.unit_chain`, `match.unit_equal`,
  `pick.equal_measures`, `sortbins.unit_kind`, `table.conversion`
* **Текстови задачи** (900–1650) — `blank.money_change`, `blank.ratio_shares`,
  `blank.recipe_pair`, `pick.equal_ratios`
* **Движение** (1100–1800) — `blank.meeting_pair`, `blank.speed_pair`,
  `table.speed_table`
* **Проценти** (1150–1750) — `blank.discount_pair`, `blank.interest_pair`,
  `match.percent_change`
* **Събиране и изваждане** (950–1450) — `blank.duration_pair`,
  `clock.start_time`
* **Числа и редици** (980–1500) — `line.temperature`,
  `sortbins.temperature_compare`
* **Остатъци** (1100–1550) — `blank.calendar_pair`
* **Работа** (1250–1700) — `blank.work_pair`
* **Подобни фигури** (1300–1750) — `match.map_scale`

Half the file is `blanks`, because a rate problem naturally has two halves —
the speed and the distance, the discount in leva and the new price, the whole
weeks and the days left over — and asking for both is what shows the method.

### 10.17 `interactive_number` — 1,636 problems, 25 families, Elo 700–1700

The largest interactive file: divisibility, primes, GCD and LCM, remainders,
fractions, percents, place value, rounding, times tables, clocks.

* **Дроби** (900–1550) — `blank.fraction_to_mixed`,
  `pick.equivalent_fractions`, `shade.fraction_grid`, `sortbins.fraction_size`
* **Числа и редици** (880–1400) — `pick.rounding_targets`,
  `sortbins.number_kind`, `table.function_machine`, `table.place_value`
* **Проценти** (1050–1700) — `blank.percent_triple`, `match.fraction_percent`,
  `pick.percent_of`, `shade.percent_grid`
* **Събиране и изваждане** (700–1500) — `blank.missing_digits_sum`,
  `clock.after_minutes`, `clock.set_time`
* **Делимост** (900–1570) — `match.number_property`, `pick.divisible`
* **Прости числа** (1000–1470) — `pick.primes`, `sortbins.prime_composite`
* **Ред на действията** (1000–1450) — `line.place_operation`,
  `match.expression_value`
* **Умножение и деление** (760–1230) — `pick.multiples_of`, `table.times_gaps`
* **НОД и НОК** (1200–1650) — `blank.gcd_lcm_pair`
* **Остатъци** (980–1430) — `blank.division_remainder`

Nothing here is a new *idea* — every type has a typed twin in `numbers` or
`fractions`. The point is the mechanic: a student who cannot yet write `10/3`
as a mixed number can still fill two boxes, and a student who cannot articulate
„кратно на 2“ can still pick the multiples out of a list.

### 10.18 `interactive_puzzles` — 1,791 problems, 30 families, Elo 1550–2800

The competition catalogue, Elo 1550–2800: magic squares, Nim, pigeonhole,
weighings, knight moves, Pick's theorem, telescoping sums. Documented type by
type in §5.

* **Логически задачи** (1550–2800) — `puzzle.addition_pyramid`,
  `puzzle.cage_sum`, `puzzle.chessboard_colour`, `puzzle.domino_parity`,
  `puzzle.hanoi_moves`, `puzzle.knight_moves`, `puzzle.latin_square`,
  `puzzle.magic_square`, `puzzle.nim_move`, `puzzle.nim_positions`,
  `puzzle.pigeonhole_pair`, `puzzle.sort_possible`, `puzzle.weighings`
* **Числа и редици** (1550–2650) — `puzzle.consecutive_sum`,
  `puzzle.cryptarithm_reverse`, `puzzle.digit_difference`,
  `puzzle.digits_sum_product`, `puzzle.fibonacci_step`, `puzzle.gauss_sum`,
  `puzzle.match_base_two`, `puzzle.square_sum_pattern`
* **Остатъци** (1550–2450) — `puzzle.last_digit_cycle`,
  `puzzle.remainder_pair`, `puzzle.weekday_shift`
* **Площ** (1550–2800) — `puzzle.lattice_rectangle`, `puzzle.pick_theorem`
* **Делимост** (1750–2650) — `puzzle.divisor_count`
* **Степени и корени** (1550–2450) — `puzzle.pick_perfect_squares`
* **Уравнения** (1750–2650) — `puzzle.diophantine_pair`
* **Дроби** (1900–2800) — `puzzle.telescoping`

### 10.19 `logic` — 458 problems, 16 families, Elo 900–1910

Counting and reasoning, typed: the multiplication principle, permutations,
combinations, handshakes, chickens-and-rabbits, pigeonhole, two-set Venn
diagrams, age and balance puzzles.

* **Логически задачи** (950–1760) — `logic.age_puzzle`, `logic.balance`,
  `logic.chickens_rabbits`, `logic.coins_mix`, `logic.odd_one_out`,
  `logic.pigeonhole`, `logic.sum_and_difference`, `logic.venn_two_sets`
* **Броене и комбинаторика** (900–1910) — `count.choose`, `count.grid_paths`,
  `count.handshakes`, `count.numbers_from_digits`, `count.outfits`,
  `count.permutations`, `count.subsets`, `count.together_pair`

`count.grid_paths` is the ancestor of §5c — the same right-and-down count on a
plain rectangle, typed and without a figure. A new path-counting family belongs
beside one of those two, not next to both.

### 10.20 `numbers` — 1,148 problems, 32 families, Elo 660–1760

Number theory as school does it: divisibility rules, divisor counts, GCD and
LCM, remainders, primes, place value, rounding, comparing, and sequences to
continue.

* **Числа и редици** (660–1460) — `numbers.build_extreme`, `numbers.compare`,
  `numbers.digit_sum`, `numbers.expanded_form`, `numbers.midpoint`,
  `numbers.negative_compare`, `numbers.place_value`, `numbers.round`,
  `numbers.sequence_multiply`, `numbers.sequence_pattern`,
  `numbers.sequence_step`
* **Делимост** (860–1510) — `divis.count_multiples`, `divis.divisors_of`,
  `divis.largest_proper`, `divis.missing_digit`, `divis.rule_2_5_10`,
  `divis.rule_3_9`
* **НОД и НОК** (1020–1740) — `gcd.pair`, `gcd.product_rule`,
  `gcd.story_pieces`, `lcm.pair`, `lcm.story_buses`
* **Остатъци** (1080–1760) — `rem.adjust_to_divide`, `rem.basic`,
  `rem.find_number`, `rem.last_digit`, `rem.weekday`
* **Прости числа** (950–1580) — `prime.count_between`, `prime.factorize`,
  `prime.identify`, `prime.next_after`, `prime.smallest_factor`

The `rem.*` group is the one that keeps paying off later — remainders are what
the weekday, last-digit and cycle problems in §5 are made of.

### 10.21 `word_problems` — 1,040 problems, 29 families, Elo 830–1860

Motion, work, ratio and shopping, plus the equations that come out of them.

* **Текстови задачи** (830–1610) — `word.average_needed`, `word.consecutive`,
  `word.ratio_split`, `word.recipe_scale`, `word.remaining_pages`,
  `word.saving_plan`, `word.share_unequally`, `word.tickets_mixed`,
  `word.two_step_shopping`
* **Уравнения** (940–1710) — `eq.both_sides`, `eq.brackets`,
  `eq.check_solution`, `eq.from_story`, `eq.linear_two_step`,
  `eq.perimeter_equation`, `eq.proportion`, `eq.with_fraction`
* **Движение** (980–1860) — `move.average_speed`, `move.catch_up`,
  `move.distance`, `move.round_trip`, `move.speed`, `move.time`, `move.towards`
* **Работа** (1050–1860) — `work.inverse_people`, `work.pipes`,
  `work.rate_time`, `work.remaining`, `work.together`

House style, enforced by nothing but habit: names, goods and containers from
`Props`, gender-neutral verbs (aorist, no participles), no possessive pronouns,
and no unrealistic quantities — the ladder's top rung raises the numbers, not
the absurdity.
